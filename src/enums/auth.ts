export enum SSOType {
  Google = 'google',
  Microsoft = 'microsoft',
  Apple = 'apple',
}

export enum DevicePlatform {
  Ios = 'ios',
  Android = 'android',
  Web = 'web',
  Electron = 'electron',
  Api = 'api',
}
