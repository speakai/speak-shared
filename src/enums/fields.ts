export enum FieldType {
  Text = 'text',
  Url = 'url',
  Boolean = 'boolean',
  Date = 'date',
  Datetime = 'datetime',
  Number = 'number',
  Currency = 'currency',
}

export enum AllowedValuesMode {
  Single = 'single',
  Multiple = 'multiple',
}

export enum DefaultViewColumn {
  Name = 'name',
  Duration = 'duration',
  Tags = 'tags',
  Sentiment = 'sentiment',
  Datetime = 'datetime',
  Size = 'size',
  MediaType = 'mediaType',
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}
