export enum RecorderAnswerType {
  Single = 'single',
  Multiple = 'multiple',
  Checkbox = 'checkbox',
  Radiobutton = 'radiobutton',
  Dropdownlist = 'dropdownlist',
  Date = 'date',
  Time = 'time',
  Datetime = 'datetime',
}

export enum RecorderUploadType {
  RECORD = 'record',
  FILE = 'file',
  YOUTUBE = 'youtube',
  LIVE_RECORD = 'live-record',
}

export enum RecordingFeedbackRating {
  POSITIVE = 'positive',
  NEGATIVE = 'negative',
}
