export enum ServiceType {
  RECORDER = 'recorder',
  PLAYER = 'player',
  LIBRARY = 'library',
}

export enum VerificationStatus {
  PENDING = 'pending',
  VERIFIED = 'verified',
  FAILED = 'failed',
  ACTIVE = 'active',
}
