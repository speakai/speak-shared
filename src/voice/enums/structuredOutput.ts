/**
 * Structured Output Enums
 */

/**
 * Supported types for structured outputs
 */
export enum StructuredOutputType {
  STRING = "string",
  BOOLEAN = "boolean",
  NUMBER = "number",
  INTEGER = "integer",
  OBJECT = "object",
  ARRAY = "array",
}
